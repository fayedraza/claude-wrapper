# Project fixtures

Reserved for custom, feature-specific Playwright fixtures (seeded state, page-level setup) once stories introduce features to fixture. Merge new fixtures into `../merged-fixtures.ts` via `mergeTests` as they're added — see `fixtures-composition.md` in the TEA knowledge base for patterns.
