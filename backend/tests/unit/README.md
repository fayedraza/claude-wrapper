# Unit tests

Reserved for pure-function/business-logic tests (e.g. `brain/`, `engine/` decision logic) once those packages have real implementations. Story 1.1 scaffolds only empty placeholder packages, so there is nothing to unit-test yet.
