# Integration tests

Reserved for tests that exercise the gateway against a real Redis instance (`docker compose up -d redis`) or other real dependencies, once a story wires one in. Story 1.1 has no such integration yet — see `tests/api/test_health.py` for the current API-level coverage.
