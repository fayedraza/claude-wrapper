"""Settings Router (FR-1-FR-3): natural-language `.claude/` config requests.

See `router.py` for classification and `apply.py` for the approval-gated
write path. `models.py` holds the structured-output contract shared by both.
"""
