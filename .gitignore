# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/

# Node
node_modules/
.next/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.coverage
.pytest_cache/
htmlcov/

# Env / secrets
.env
.env.local
.env.*.local

# Redis / local data
*.rdb
*.aof

# BMad Claude Code runtime state
.claude/scheduled_tasks.lock

# Story 1.4: personal permission grants -- a personal trust decision, never
# team-shared (Boundaries: reads/writes never touch .claude/settings.json).
.claude/settings.local.json

# Playwright
/test-results/
/playwright-report/
/blob-report/
tests/.auth-sessions/
_bmad-output/test-artifacts/playwright-junit.xml
# Defensive: @seontechnologies/playwright-utils's configureAuthSession() has
# ignored a custom storage path before and written here instead (upstream
# bug — see tests/global-setup.ts).
/.auth/
